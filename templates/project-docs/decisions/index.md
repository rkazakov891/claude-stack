# Архитектурные решения (ADR)

> Каталог решений. Каждая строка — один файл в этой папке. Хронология сверху вниз (новые добавляются ниже).

| Дата | Решение | Статус |
|------|---------|--------|
| | | |

---

**Статусы:**
- `accepted` — действует
- `superseded` — заменено более новым (см. поле `supersedes`)
- `reverted` — откатили, не применяется

**Создание нового ADR:** `/document decision <slug>`
