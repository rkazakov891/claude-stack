# CLAUDE.md — PROJECT_NAME

Per-project руководство для Claude Code. Дополняет глобальный `~/.claude/CLAUDE.md`, не отменяет.

<!-- BEGIN INHERITED OPERATING RULES (auto-synced from ~/.claude/CLAUDE.md, do not edit by hand) -->

> 🔴 **Inherited operating rules** from `~/.claude/CLAUDE.md` (single source of truth).
> Do not edit between the markers — changes will be overwritten on the next sync.
> Refresh via `python ~/.claude/scripts/sync-operating-rules.py <this file>` or `--all` to sweep every project.
>
> **The block below is left empty in the template** — `/new-project` runs the sync script immediately after copying this file, which fills it with the current rules from the global `CLAUDE.md`. The PostToolUse hook keeps it fresh thereafter.

<!-- END INHERITED OPERATING RULES -->

## Контекст

PROJECT_DESCRIPTION

## Стек

<заполнится по мере появления первого кода>

## Запуск (порты)

<заполнится при первом запуске сервиса; помни — порт 8000 не использовать>

## Команды

<копируемые команды для типичных операций: ETL, dev-сервер, тесты, docs site>

## Правила работы

- См. inherited operating rules выше — они источник истины.
- Локальные дополнения перечисляй здесь по мере появления (что-то специфичное для этого проекта, чего нет в глобальных правилах).

## Что точно НЕ делать

<проектно-специфичные запреты, если есть>

## Связи

<ссылки на парные проекты, репо, доски, документы>
